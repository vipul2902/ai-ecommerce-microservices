# Tests

**Status:** Empty placeholder — no test projects yet.

This directory will hold automated test projects (unit, integration, and
contract tests) mirroring the services under [src/](../src/). Test projects
will be added alongside each service as it is implemented, not added
speculatively ahead of time.
