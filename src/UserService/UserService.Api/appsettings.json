{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Set UserServiceDb via 'dotnet user-secrets' (local dev) or the ConnectionStrings__UserServiceDb environment variable. Never commit a real value here.",
    "UserServiceDb": ""
  },
  "Jwt": {
    "_comment": "Set Key via 'dotnet user-secrets' (local dev) or the Jwt__Key environment variable. Never commit a real value here.",
    "Issuer": "UserService",
    "Audience": "AiEcommercePlatform",
    "Key": "",
    "ExpiryMinutes": 60
  }
}
