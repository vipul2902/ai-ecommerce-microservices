{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Set ProductServiceDb via 'dotnet user-secrets' (local dev) or the ConnectionStrings__ProductServiceDb environment variable. Never commit a real value here.",
    "ProductServiceDb": ""
  }
}
